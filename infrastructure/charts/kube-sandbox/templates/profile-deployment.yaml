{{- if .Values.apps.profile.enabled -}}
{{- $ctx := . -}}
{{- $app := .Values.apps.profile -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "kube-sandbox.appFullname" (dict "ctx" $ctx "appName" "profile") }}
  labels:
    {{- include "kube-sandbox.labels" $ctx | nindent 4 }}
    app.kubernetes.io/component: profile
spec:
  replicas: {{ $app.replicaCount }}
  selector:
    matchLabels:
      {{- include "kube-sandbox.appSelectorLabels" (dict "ctx" $ctx "appName" "profile") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "kube-sandbox.appSelectorLabels" (dict "ctx" $ctx "appName" "profile") | nindent 8 }}
      {{- with $ctx.Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      {{- with $ctx.Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: profile
          image: {{ include "kube-sandbox.image" (dict "ctx" $ctx "appName" "profile") }}
          imagePullPolicy: {{ $app.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ $app.port }}
              protocol: TCP
          env:
            - name: PORT
              value: {{ $app.port | quote }}
          {{- if $app.healthcheckPath }}
          readinessProbe:
            httpGet:
              path: {{ $app.healthcheckPath }}
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: {{ $app.healthcheckPath }}
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          {{- end }}
          {{- with $ctx.Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml $app.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-sandbox.appFullname" (dict "ctx" $ctx "appName" "profile") }}
  labels:
    {{- include "kube-sandbox.labels" $ctx | nindent 4 }}
    app.kubernetes.io/component: profile
spec:
  type: ClusterIP
  selector:
    {{- include "kube-sandbox.appSelectorLabels" (dict "ctx" $ctx "appName" "profile") | nindent 4 }}
  ports:
    - name: http
      port: {{ $app.port }}
      targetPort: http
      protocol: TCP
{{- end }}
